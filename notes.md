# Scripts Notes
Notes for scripts along with why they're used instead of alias nor doskey entries.

## Why not alias nor doskey
I am using short named scripts instead of doskey entries for:
    - ability to chain commands on Windows where it doesn't have be the first command
    - usage and much more logic
    - running them from within other scripts
    - having the same shortcut "commands" Mac/Linux/Windows
