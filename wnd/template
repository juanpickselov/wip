@ECHO OFF
IF /I "%1" == "-h" GOTO usage
IF /I "%1" == "/?" GOTO usage
IF [%1] NEQ [] GOTO usage

REM commands go here before the call to EOF
GOTO :EOF

:usage
ECHO ------------------------------------
ECHO Usage        : command [args]
ECHO Description  : description here
ECHO ------------------------------------
EXIT /b 1
